<form action="">
  <div class="row md:grid-cols-2">
    <div>
      <label for="first-name">First Name: <sup>*</sup></label>
      <input type="text" id="first-name" name="first-name" required />
    </div>
    <div>
      <label for="last-name">Last Name:</label>
      <input type="text" id="last-name" name="last-name" />
    </div>
  </div>
  <div class="row">
    <div class="md:w-1/2">
      <label for="email">E-mail: <sup>*</sup></label>
      <input type="email" id="email" name="email" required />
    </div>
  </div>
  <div class="row">
    <div>
      <label for="studie-question">
        Have you studied with Geshe Tenzin Gelek before? <sup>*</sup>
      </label>
      <select name="studie-question" id="studie-question" required>
        <option value="" disabled selected>Please select</option>
        <option value="yes">Yes</option>
        <option value="no">No</option>
      </select>
    </div>
  </div>
  <div class="row">
    <div>
      <label for="administrative-questions">
        Any administrative questions you might have, payment plan or limited
        scholarship inquiry:
      </label>
      <textarea
        name="administrative-questions"
        id="administrative-questions"
        rows="5"
      ></textarea>
      <p class="px-1">
        <small>
          * Payment plan options & limited scholarship inquiry, please write
          here.
        </small>
      </p>
    </div>
  </div>
  <div class="row">
    <div>
      <label for="donation">Donation: <sup>*</sup></label>
      <input type="number" id="donation" name="donation" value="200" required />
      <p class="px-1"><small>Suggested donation: $200</small></p>
    </div>
  </div>
  <div class="row">
    <div class="info">
      <p>
        <strong class="block">Important note:</strong>
        If the payment works, you should get the confirmation screen, and from that
        confirmation screen, you would need to click the Return to <strong>Merchant button</strong
        >
        to get your form's thank you message. That's it!
      </p>
    </div>
  </div>
  <button type="submit">Register</button>
</form>

<style>
  label {
    @apply block px-1 mb-1;
  }

  input,
  select,
  textarea {
    @apply block w-full p-2 border border-neutral-2 rounded-md;
  }

  select {
    @apply md:w-1/2;
  }

  sup {
    @apply top--1 text-size-lg fw-bold text-red-6;
  }

  button {
    @apply inline-block rounded-lg px-15 py-3 text-xl font-bold bg-blue hover:bg-neutral-9 c-white transition-all w-full md:w-auto;
  }

  .row {
    @apply grid gap-5 mb-10;
  }

  .info {
    @apply bg-sky-1 p-3 rounded-md;
  }
</style>
